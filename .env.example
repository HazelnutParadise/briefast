# 複製為 .env 後填入實際值。.env 不進版控。
# Docker Compose 會自動讀取本目錄的 .env，不需要額外參數。
# 本機直接跑 go run . 時 Go 程式不會解析 .env，請先載入環境：
#   set -a; source .env; set +a

# 後台管理員密碼。未設定時公開頁與 API 照常服務，後台會拒絕登入並顯示設定錯誤。
BRIEFAST_ADMIN_PASSWORD=replace-with-a-long-random-password

# 站台對外網址，供 canonical、Open Graph 與 sitemap 產生絕對網址。
# 未設定時會依請求的 Host 與 X-Forwarded-Proto 推導；正式部署請務必設定，
# 反向代理沒送 X-Forwarded-Proto 時推導結果會退化成 http。
# BRIEFAST_SITE_URL=https://briefast.example.com

# Compose 對外埠，預設 8600。
# BRIEFAST_PORT=8600

# 資料庫落地位置（Compose）。預設用 named volume briefast-data；
# 想放在主機特定目錄就填路徑，例如 /srv/briefast/data 或 ./data。
# BRIEFAST_DATA=briefast-data

# SQLite 路徑，只在本機直接跑 go run 時有用，預設 data/briefast.db。
# 容器內固定為 /app/data/briefast.db，不需設定。
# BRIEFAST_DB_PATH=data/briefast.db
