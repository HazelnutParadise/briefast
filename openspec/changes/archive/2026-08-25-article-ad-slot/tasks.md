## 1. 版位標記與樣式

- [x] 1.1 先寫失敗測試，交付 `Advertising slot` 的標記位置：以 `sy.NewAppTest` 取得報告頁的 HTML，斷言廣告掛載點出現在個股多空判斷區塊之後、產業新聞摘要區塊之前，且帶有 `廣告` 標示。同時斷言歷史列表頁、查無報告的日期頁與等待報告的首頁都不含該掛載點。驗證方式：`go test ./internal/site/` 因掛載點尚未存在而失敗。
- [x] 1.2 在 `internal/site/site.go` 的報告版面中，於個股多空判斷與產業新聞摘要之間輸出廣告版位標記，包含髮絲規線、`廣告` 標示與廣告服務要求的掛載點元素。只在有報告內容的版面輸出。驗證方式：1.1 的測試全數轉綠。
- [x] 1.3 在 `internal/site/styles.go` 加入版位樣式，交付 `Slot is labelled and delimited`：零圓角、零陰影，上下以髮絲規線界定，`廣告` 標示採用既有的小標籤字級與字距，顏色用次要文字色。驗證方式：瀏覽器實際檢視，確認版位在視覺上是獨立區塊而非內文的一部分。

## 2. 載入廣告

- [x] 2.1 交付 `Loader runs in the main document` 與 `Background update does not re-run the loader`：在報告頁的版面之後以 `sy.Embed` 承載廣告 loader script，並給定固定的 key，使背景推送觸發的重新渲染重用既有節點、不重跑 script。驗證方式：瀏覽器開啟報告頁確認 loader 取得掛載點並完成初始化；再以管理介面或 API 觸發一次報告更新，確認頁面更新後廣告節點未重新請求。

## 3. 設計文件與收尾

- [x] 3.1 在 `DESIGN.md` 記錄廣告版位的處理原則：第三方內容不受本專案視覺規則約束，因此以規線與標示界定邊界，並說明它與「不加行銷 CTA」那條規則的分野，避免日後被誤刪。驗證方式：閱讀該檔，確認版位有對應條目且與現有 Components 段落風格一致。
- [x] 3.2 端對端確認無回歸：`go vet ./...` 與 `go test ./...` 全數通過，報告頁版面其餘區塊、免責聲明、歷史頁與後台行為不變。驗證方式：保留指令輸出與瀏覽器截圖作為憑據。
